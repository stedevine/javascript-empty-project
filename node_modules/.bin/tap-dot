#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../tap-dot/bin/dot" "$@"
  ret=$?
else 
  node  "$basedir/../tap-dot/bin/dot" "$@"
  ret=$?
fi
exit $ret
